import Util

w=0.25
print(f"Entropia para valor {w} es {Util.valor_W(w):.2f}")

w=0.75
print(f"Entropia para valor {w} es {Util.valor_W(w):.2f}")

w=0.5
print(f"Entropia para valor {w} es {Util.valor_W(w):.2f}")

w=1
print(f"Entropia para valor {w} es {Util.valor_W(w):.2f}")

w=0
print(f"Entropia para valor {w} es {Util.valor_W(w):.2f}")